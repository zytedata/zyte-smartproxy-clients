name: php-7
on: push
env:
  PROXY: proxy.crawlera.com:8010
  URL: https://scrapy.org/
  KEY: ${{secrets.KEY}}
jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: php-7
        run: make php-7
